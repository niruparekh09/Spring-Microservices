spring:
  application:
    name: Greet-API
  boot:
    admin:
      client:
        url: http://localhost:1111/
server:
  port: 9082
management:
  endpoints:
    web:
      exposure:
        include: '*'